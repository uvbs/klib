#include "stdafx.h"

#include "StringUtils.h"
#include <iostream>
#include <ostream>
#include <algorithm>
#include <vector>
#include <string>
#include <istream>

using namespace std;



int main()
{
//	cout << "Hola, Mundo!" << endl;
    /*
	char myStr[256];
	std::string sep;
	cout << "Introduzca la cadena: ";
	cin >> myStr;
	cout << "Introduzca el separador: ";
	cin >> sep;
	cout << endl;

	std::vector<std::string> v = StringUtils::Split(myStr,sep);
	cout << "TEXTO ORIGINAL: " << myStr << endl << endl;

	cout << "StringUtils::Split: " << endl;
	std::copy(v.begin(), v.end(), std::ostream_iterator<std::string>(cout, "\n"));

	std::string szJoined = StringUtils::ProperCase(StringUtils::Join(v));
	cout << endl << "StringUtils::ProperCase + StringUtils::Join: " << szJoined << endl;
	cout << "StringUtils::ToUpper: " << StringUtils::ToUpper(szJoined) << endl;
	cout << "StringUtils::ToLower: " << StringUtils::ToLower(szJoined) << endl;
	std::string szReemp;
	cout << "Introduzca el string de reemplazo: ";
	cin >> szReemp;
	cout << "StringUtils::Replace: " << StringUtils::Replace(szJoined,' ',szReemp) << endl;
    */

	return 0;
}